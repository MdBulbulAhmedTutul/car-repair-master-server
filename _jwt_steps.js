/**
 * 1. install json wentoken
 * 2. require jwt = require('jsonwebtoken');
 * 3. jwt.sign(payload, secret, {expiresIn: '1h'})
 * 4. client side npm install axios
*/

/**
 * how to store token in the client side
 * 1. memory --> ok type
 * 2. local storage --> ok type (XSS)
 * 3.  cookies: http only
 * */ 